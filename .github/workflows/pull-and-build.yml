name: Pull from Private & Release Open Core

on:
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Tipo de incremento de versão (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  sync-build-release:
    name: Sync, Build & Release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            goos: linux
            goarch: amd64
            suffix: -linux
          - os: macos-latest
            goos: darwin
            goarch: amd64
            suffix: -mac
          - os: macos-latest
            goos: darwin
            goarch: arm64
            suffix: -mac-arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
            suffix: .exe

    steps:
      - name: Checkout Public Repository
        uses: actions/checkout@v4
        with:
          path: public-repo
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Checkout Private Repository
        uses: actions/checkout@v4
        with:
          repository: Filipe-Lunfuankenda/pwaa
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: private-repo
          fetch-depth: 0
          
      - name: Sincronizar Ficheiros Core (Só no Ubuntu)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          find public-repo -mindepth 1 -maxdepth 1 ! -name '.git' ! -name 'README.md' ! -name '.github' -exec rm -rf {} +
          mkdir -p public-repo/sdk
          rsync -av --exclude='*.exe' --exclude='*-linux*' --exclude='*-mac*' --exclude='*.aar' --exclude='*.xcframework' private-repo/sdk/ public-repo/sdk/ 2>/dev/null || true
          rsync -av --exclude='*.exe' --exclude='*-linux*' --exclude='*-mac*' --exclude='*.aar' --exclude='*.xcframework' private-repo/reader/ public-repo/sdk/reader/
          cp -r private-repo/examples public-repo/ 2>/dev/null || true
          cp -r private-repo/assets public-repo/ 2>/dev/null || true
          cp private-repo/PWAA_DOCUMENTATION.md public-repo/ 2>/dev/null || true

          cd public-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          if ! git diff --staged --quiet; then
            git commit -m "Automated Open Core Sync from Private Repo"
            for attempt in 1 2 3; do
              git pull --rebase origin "${GITHUB_REF_NAME}"
              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                break
              fi
              if [ "$attempt" -eq 3 ]; then
                echo "Failed to push synchronized files after 3 attempts."
                exit 1
              fi
              sleep 2
            done
          fi

      - name: Determinar Nova Versão (Só no Ubuntu)
        if: matrix.os == 'ubuntu-22.04'
        id: versioning
        run: |
          cd private-repo
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          
          # 1. Limpar espaços, quebras de linha e o prefixo 'v'
          LATEST_TAG=$(echo "$LATEST_TAG" | tr -d '[:space:]')
          VERSION=${LATEST_TAG#v}
          echo "Versão anterior no Privado limpa: $VERSION"
          
          # 2. Separar em Major, Minor e Patch
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
          
          # 3. Forçar a ser numérico (caso venha vazio, assume 0)
          MAJOR=${MAJOR:-0}
          MINOR=${MINOR:-0}
          PATCH=${PATCH:-0}
          
          BUMP="${{ github.event.inputs.version_bump }}"
          if [ "$BUMP" = "major" ]; then
            MAJOR=$((MAJOR+1))
            MINOR=0
            PATCH=0
          elif [ "$BUMP" = "minor" ]; then
            MINOR=$((MINOR+1))
            PATCH=0
          else
            PATCH=$((PATCH+1))
          fi
          
          NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
          echo "Nova versão calculada: $NEW_TAG"
          echo "NEW_TAG=$NEW_TAG" >> $GITHUB_ENV
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag $NEW_TAG
          git push origin $NEW_TAG

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          cache: true

      - name: Install Linux Dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev

      - name: Build PWAA Reader Core
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 1
        run: |
          cd private-repo/reader
          go build -v -trimpath -ldflags="-s -w" -o ../../pwaareader${{ matrix.suffix }} .

      - name: Build PWAA Builder
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 1
        run: |
          cd private-repo/builder
          go build -v -trimpath -ldflags="-s -w" -o ../../pwaa-builder${{ matrix.suffix }} .

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pwaa-tools${{ matrix.suffix }}
          path: |
            pwaareader${{ matrix.suffix }}
            pwaa-builder${{ matrix.suffix }}
          retention-days: 1

  build-mobile:
    name: Build PWAA Mobile SDK
    runs-on: macos-latest
    steps:
      - name: Checkout Private Repository
        uses: actions/checkout@v4
        with:
          repository: Filipe-Lunfuankenda/pwaa
          token: ${{ secrets.PRIVATE_REPO_PAT }}
          path: private-repo

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          cache: true

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          gomobile init

      - name: Build Android Library (AAR)
        run: |
          cd private-repo/mobile
          gomobile bind -v -target=android -androidapi 21 -trimpath -ldflags="-s -w" -o ../../pwaa-sdk.aar .

      - name: Build iOS Library (XCFramework)
        run: |
          cd private-repo/mobile
          gomobile bind -v -target=ios -trimpath -ldflags="-s -w" -o ../../pwaa-sdk.xcframework .

      - name: Upload Mobile Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pwaa-mobile-sdk
          path: |
            *.aar
            *.xcframework
          retention-days: 1

  release:
    name: Criar Release Pública
    needs: [sync-build-release, build-mobile]
    runs-on: ubuntu-latest
    steps:
      - name: Download all workflow artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release ZIPs
        run: |
          mkdir release_assets
          for dir in artifacts/*/; do
            if [ -d "$dir" ]; then
              base=$(basename "$dir")
              zip -r "release_assets/${base}.zip" "$dir"
            fi
          done

      - name: Gerar Tag de Release
        id: get_tag
        run: |
          git clone https://${{ secrets.PRIVATE_REPO_PAT }}@github.com/Filipe-Lunfuankenda/pwaa.git private-repo
          cd private-repo
          LATEST_TAG=$(git describe --tags --abbrev=0)
          echo "TAG=$LATEST_TAG" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG }}
          files: release_assets/*.zip
          token: ${{ secrets.GITHUB_TOKEN }}
